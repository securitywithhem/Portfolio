/**
 * Extra pinned GitHub repos to surface in the GitHub section, in addition to
 * any project that already carries a `github` URL (see data/projects.ts).
 *
 * Dharma and VaultIQ are the GRC and AI Security proof points and already
 * provide their repo URLs via data/projects.ts, so they surface automatically
 * and don't need to be repeated here. Add any additional showcase repos below.
 *
 * Format: full GitHub repo URL (https://github.com/owner/repo)
 */
export const pinnedRepos = [] as const;
