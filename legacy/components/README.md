# /components

Conventions live in [Docs/COMPONENT_ARCHITECTURE.md](../Docs/COMPONENT_ARCHITECTURE.md):
`ui/` = shadcn primitives, `providers/` = client context providers,
`shared/` = cross-section reusables, `layout/` = structural pieces,
`sections/` = page sections (Phases 2–4). Server Components by default;
tokens only, no invented design values.
